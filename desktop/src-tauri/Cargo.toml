[package]
name = "openbot-desktop"
version = "0.0.12"
description = "OpenBot Desktop"
edition = "2021"
rust-version = "1.77"

[lib]
name = "openbot_desktop_lib"
path = "src/lib.rs"

[[bin]]
name = "openbot-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.9"
base64 = "0.22"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "blocking", "json"] }
flate2 = "1"
# Checking the digest of an installer before running it. See `install.rs`.
sha2 = "0.10"
tar = "0.4"
# Read Bun release ZIPs in-process; fresh Windows installs must not depend on a shell extractor.
zip = { version = "2.4.2", default-features = false, features = ["deflate-flate2", "flate2"] }
tauri-plugin-single-instance = "2.0.0-rc.5"
portable-pty = "0.9.0"
tauri-plugin-opener = "2.5.5"
tauri-plugin-dialog = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tauri = { version = "2", features = ["test"] }
